const pool = require('./config/db'); async function run() { try { const u1 = await pool.query(INSERT INTO users (first_name, last_name, email, password_hash, role, contact_numbers, is_verified) VALUES ('Juan', 'Dela Cruz', 'juan' + Date.now() + '@example.com', 'dummyhash', 'student', '09123456789', true) RETURNING id); const uid1 = u1.rows[0].id; const u2 = await pool.query(INSERT INTO users (first_name, last_name, email, password_hash, role, contact_numbers, is_verified) VALUES ('Maria', 'Clara', 'maria' + Date.now() + '@example.com', 'dummyhash', 'student', '09987654321', true) RETURNING id); const uid2 = u2.rows[0].id; const cRes = await pool.query(SELECT id, name FROM courses WHERE category ILIKE '%PDC%' LIMIT 1); if (cRes.rows.length === 0) throw new Error('No PDC course found.'); const cid = cRes.rows[0].id; const cName = cRes.rows[0].name; await pool.query(INSERT INTO bookings (user_id, course_id, course_type, status, total_amount) VALUES (, , 'Manual', 'paid', 1000), [uid1, cid]); await pool.query(INSERT INTO bookings (user_id, course_id, course_type, status, total_amount) VALUES (, , 'Automatic', 'paid', 1000), [uid2, cid]); const bRes = await pool.query(SELECT id FROM branches LIMIT 1); const bid = bRes.rows[0].id; const sRes1 = await pool.query(INSERT INTO schedule_slots (branch_id, type, date, session, time_range, total_capacity, available_slots, course_type, transmission) VALUES (, 'PDC', CURRENT_DATE - INTERVAL '2 days', 'Morning', '08:00 AM - 12:00 PM', 5, 3, , 'Manual') RETURNING id, [bid, cName]); const sid1 = sRes1.rows[0].id; await pool.query(INSERT INTO schedule_enrollments (slot_id, student_id, enrollment_status) VALUES (, , 'no-show'), [sid1, uid1]); await pool.query(INSERT INTO schedule_enrollments (slot_id, student_id, enrollment_status) VALUES (, , 'no-show'), [sid1, uid2]); const sRes2 = await pool.query(INSERT INTO schedule_slots (branch_id, type, date, session, time_range, total_capacity, available_slots, course_type, transmission) VALUES (, 'PDC', CURRENT_DATE + INTERVAL '2 days', 'Afternoon', '01:00 PM - 05:00 PM', 5, 5, , 'Manual') RETURNING id, [bid, cName]); console.log('Successfully created Dummy Students!'); process.exit(0); } catch(err) { console.error(err); process.exit(1); } } run();
