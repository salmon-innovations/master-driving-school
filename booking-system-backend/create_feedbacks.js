const pool = require('./config/db'); async function migrate() { try { await pool.query("CREATE TABLE IF NOT EXISTS feedbacks ( id SERIAL PRIMARY KEY, user_id INTEGER REFERENCES users(id) ON DELETE CASCADE, course_name VARCHAR(255), rating INTEGER NOT NULL CHECK (rating >= 1 AND rating <= 5), comment TEXT NOT NULL, video_url TEXT, is_public BOOLEAN DEFAULT true, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP );"); console.log('Created feedbacks table'); } catch (e) { console.error('Error:', e); } finally { process.exit(); } } migrate();
