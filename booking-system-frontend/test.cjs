require.extensions['.css'] = () => {}; require.extensions['.svg'] = () => ''; require('@babel/register')({presets: ['@babel/preset-react']}); require('jsdom-global')(); const React = require('react'); const { renderToString } = require('react-dom/server'); const Admin = require('./src/admin/Admin.jsx').default; console.log('Successfully loaded Admin, trying to render...'); try { renderToString(React.createElement(Admin)); console.log('Render successful!'); } catch (e) { console.error('RENDER ERROR:', e); }
